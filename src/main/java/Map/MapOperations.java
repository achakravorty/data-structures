package Map;

import java.util.HashMap;

public class MapOperations {

    //In Java, the java.util.Map interface represents a collection of
    // key-value pairs where each key is associated with exactly one
    // value. It's important to note that a key in a map must be
    // unique, but values can be duplicated.

    /*
    //Adding Entries//
    Map<KeyType, ValueType> map = new HashMap<>(); // Example using HashMap

    // Adding a key-value pair
    map.put(key, value);

    // Adding multiple key-value pairs
    map.putAll(otherMap);

    //Getting Values//
    ValueType value = map.get(key);

    // Check if a key exists
    boolean containsKey = map.containsKey(key);


    //Removing Entries//
    ValueType removedValue = map.remove(key);

    //Checking if the Map is Empty//
    boolean isEmpty = map.isEmpty();

    //Size of the Map//
    int size = map.size();

    //Iterating through the Map//

    // Iterating through keys
    for (KeyType key : map.keySet()) {
        // Access the value using map.get(key)
    }

    // Iterating through values
    for (ValueType value : map.values()) {
        // Access the value
    }

    // Iterating through key-value pairs
    for (Map.Entry<KeyType, ValueType> entry : map.entrySet()) {
        KeyType key = entry.getKey();
        ValueType value = entry.getValue();
    }

    //Checking Equality//
    boolean areEqual = map1.equals(map2);

    //Getting Keys or Values as Collection//
    Collection<KeyType> keys = map.keySet();
    Collection<ValueType> values = map.values();

    //Getting a Set of Entries//
    Set<Map.Entry<KeyType, ValueType>> entrySet = map.entrySet();
     */


}
